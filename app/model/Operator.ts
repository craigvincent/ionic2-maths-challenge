export class Operator {
  symbol: String;
  verb: String;
  name: String;

  constructor() {
  }

  /*
  public actOn(a: Number, b: Number) {
    return eval([a, this.symbol, b].join(' '));
  }
  */
}
